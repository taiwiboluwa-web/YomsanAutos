import { useEffect, useMemo, useState } from 'react';
import { Grid3X3, List, SlidersHorizontal, X } from 'lucide-react';
import { AnimatePresence } from 'motion/react';
import { vehicles as seedVehicles, formatPrice, type Vehicle } from '../../data/vehicles';
import { VehicleConfigurator } from '../components/showroom/VehicleConfigurator';
import { PageShell } from '../components/navigation/SiteChrome';

type Filter='All'|'SUV'|'Sedan'|'Coupe'|'Available';
type ViewMode='list'|'grid';

export default function Inventory(){
  const [vehicles,setVehicles]=useState<Vehicle[]>(seedVehicles); const [filter,setFilter]=useState<Filter>('All');
  const [viewMode,setViewMode]=useState<ViewMode>('grid'); const [selected,setSelected]=useState<Vehicle|null>(null);
  const [brand,setBrand]=useState('All brands'); const [km,setKm]=useState('Any mileage'); const [engine,setEngine]=useState('All engines');
  useEffect(()=>{let active=true; fetch(`/api/vehicles?refresh=${Date.now()}`,{cache:'no-store'}).then(r=>r.ok?r.json():null).then(d=>{if(active&&Array.isArray(d?.vehicles))setVehicles(d.vehicles)}).catch(()=>undefined); return()=>{active=false}},[]);
  const brands=useMemo(()=>['All brands',...Array.from(new Set(vehicles.map(v=>v.brand))).sort()],[vehicles]);
  const engines=useMemo(()=>['All engines',...Array.from(new Set(vehicles.map(v=>v.engine))).sort()],[vehicles]);
  const filtered=useMemo(()=>vehicles.filter(v=>{
    if(filter!=='All'&&filter!=='Available'&&v.type!==filter)return false;
    if(filter==='Available'&&v.status!=='Available')return false;
    if(brand!=='All brands'&&v.brand!==brand)return false;
    if(engine!=='All engines'&&v.engine!==engine)return false;
    if(km==='Under 50,000'&&v.mileage>=50000)return false;
    if(km==='50,000–100,000'&&(v.mileage<50000||v.mileage>100000))return false;
    if(km==='Over 100,000'&&v.mileage<=100000)return false;
    return true;
  }),[vehicles,filter,brand,engine,km]);
  const clearFilters=()=>{setFilter('All');setBrand('All brands');setKm('Any mileage');setEngine('All engines')};
  const hasExtraFilters=brand!=='All brands'||km!=='Any mileage'||engine!=='All engines';
  return <PageShell><section className="inventory inventory-page">
    <style>{` .inventory__toolbar{display:flex;align-items:center;justify-content:space-between;gap:24px;margin-top:48px}.inventory__filters{display:flex;flex-wrap:wrap;gap:0;border-bottom:1px solid #333}.inventory__filters button{background:none;border:0;border-bottom:2px solid transparent;color:#777;padding:13px 20px;font-size:9px;letter-spacing:.13em;text-transform:uppercase;cursor:pointer}.inventory__filters button:first-child{padding-left:0}.inventory__filters button.active{color:#f2f0ea;border-bottom-color:#f2f0ea}.inventory__view-toggle{display:flex;gap:2px;border:1px solid #333;padding:3px;flex-shrink:0}.inventory__view-toggle button{display:grid;place-items:center;width:38px;height:34px;border:0;background:transparent;color:#777;cursor:pointer}.inventory__view-toggle button.active{background:#f2f0ea;color:#090a0b}.inventory__finder{margin-top:58px;padding:42px 0 36px;border-top:1px solid #333;border-bottom:1px solid #333}.inventory__finder-head{display:flex;justify-content:space-between;align-items:end;gap:30px;margin-bottom:28px}.inventory__finder-head h2{font-size:clamp(30px,4vw,58px);line-height:.92;letter-spacing:-.055em;max-width:700px;margin:8px 0 0}.inventory__finder-head h2 i{font-family:Georgia,serif;font-weight:400}.inventory__finder-kicker{font-size:9px;letter-spacing:.16em;text-transform:uppercase;color:#777}.inventory__finder-count{text-align:right;color:#777;font-size:9px;letter-spacing:.12em;text-transform:uppercase}.inventory__finder-count strong{display:block;color:#f2f0ea;font-size:28px;letter-spacing:-.04em}.inventory__selects{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.inventory__select{position:relative}.inventory__select label{display:block;font-size:8px;letter-spacing:.14em;text-transform:uppercase;color:#777;margin:0 0 8px}.inventory__select select{width:100%;appearance:none;background:#111315;color:#f2f0ea;border:1px solid #333;border-radius:0;padding:17px 42px 17px 16px;font:inherit;font-size:11px;cursor:pointer}.inventory__select svg{position:absolute;right:15px;bottom:17px;color:#777;pointer-events:none}.inventory__clear{margin-top:16px;border:0;background:none;color:#888;text-transform:uppercase;letter-spacing:.12em;font-size:8px;padding:0;cursor:pointer}.inventory__clear:hover{color:#f2f0ea}.inventory-list{display:grid;gap:1px;background:#333;margin-top:24px}.inventory-item{display:grid;grid-template-columns:30% 1fr auto;background:#0b0c0e;color:#f2f0ea;min-height:250px}.inventory-item__visual{position:relative;display:block;width:100%;min-height:250px;padding:0;border:0;overflow:hidden;background:#171819;cursor:pointer}.inventory-item__visual img{width:100%;height:100%;min-height:250px;object-fit:cover;display:block;transition:transform .5s ease}.inventory-item__visual:hover img{transform:scale(1.035)}.inventory-status{position:absolute;left:16px;top:16px;background:#f2f0ea;color:#090a0b;padding:7px 10px;font-size:8px;letter-spacing:.12em;text-transform:uppercase}.inventory-item__main{display:flex;justify-content:space-between;gap:40px;padding:34px 36px}.inventory-item__identity small,.inventory-item__price small{font-size:8px;letter-spacing:.15em;color:#777}.inventory-item__identity h2{font-size:clamp(30px,3.5vw,54px);line-height:.86;letter-spacing:-.055em;margin:20px 0 0}.inventory-item__identity h2 i{font-family:Georgia,serif;font-weight:400}.inventory-item__price{text-align:right;display:grid;align-content:start;gap:7px}.inventory-item__price strong{font-size:20px;font-weight:400}.inventory-item__price span{font-size:10px;color:#888}.inventory-item__specs{display:flex;align-items:center;gap:24px;padding:30px 28px;border-left:1px solid #333}.inventory-item__specs span{font-size:8px;letter-spacing:.12em;color:#777;white-space:nowrap}.inventory-item__specs b{color:#f2f0ea;font-size:12px;font-weight:400}.inventory-item__specs button{display:flex;align-items:center;gap:8px;border:0;border-bottom:1px solid #666;background:none;color:#f2f0ea;padding:0 0 7px;font-size:8px;letter-spacing:.12em;text-transform:uppercase;white-space:nowrap;cursor:pointer}.inventory-list--grid{grid-template-columns:repeat(4,minmax(0,1fr))}.inventory-list--grid .inventory-item{display:flex;flex-direction:column;min-height:0}.inventory-list--grid .inventory-item__visual{aspect-ratio:16/10;min-height:0}.inventory-list--grid .inventory-item__visual img{min-height:0}.inventory-list--grid .inventory-item__main{padding:20px;min-height:145px;gap:16px}.inventory-list--grid .inventory-item__identity h2{font-size:clamp(24px,2.3vw,38px)}.inventory-list--grid .inventory-item__price strong{font-size:16px}.inventory-list--grid .inventory-item__specs{padding:16px 20px;border-left:0;border-top:1px solid #333;justify-content:space-between;gap:12px}.inventory-list--grid .inventory-item__specs span:nth-child(2){display:none}.inventory-list--grid .inventory-item__specs button{font-size:7px}.inventory-empty{grid-column:1/-1;padding:60px;text-align:center;background:#0b0c0e;color:#777}@media(max-width:1200px){.inventory-list--grid{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:900px){.inventory__finder-head{align-items:flex-start;flex-direction:column}.inventory__finder-count{text-align:left}.inventory__selects{grid-template-columns:1fr}.inventory__toolbar{align-items:flex-start;flex-direction:column}.inventory__view-toggle{align-self:flex-end}.inventory-item{grid-template-columns:1fr}.inventory-item__visual{min-height:220px;aspect-ratio:16/10}.inventory-item__visual img{min-height:0}.inventory-item__main{padding:24px;gap:20px}.inventory-item__specs{padding:18px 24px;border-left:0;border-top:1px solid #333;flex-wrap:wrap}.inventory-list--grid{grid-template-columns:repeat(2,minmax(0,1fr))}.inventory-list--grid .inventory-item__specs span:nth-child(2){display:block}}@media(max-width:560px){.inventory-list--grid{grid-template-columns:1fr}.inventory__finder{margin-top:40px;padding-top:30px}.inventory__finder-head h2{font-size:34px}}`}</style>
    <div className="inventory__top"><div><p className="eyebrow">YOMSAN AUTOMOBILE / LIVE STOCK</p><h1>Inventory</h1><p className="inventory__intro">Current Yomsan vehicles in Lagos. Filter the stock, compare the essentials and open any vehicle for its full showroom presentation.</p></div><div className="inventory__count"><strong>{String(filtered.length).padStart(2,'0')}</strong><span>vehicles shown</span></div></div>
    <section className="inventory__finder"><div className="inventory__finder-head"><div><span className="inventory__finder-kicker">Find your next vehicle</span><h2>Let’s get you <i>behind the wheel</i> of your next vehicle.</h2></div><div className="inventory__finder-count"><strong>{String(filtered.length).padStart(2,'0')}</strong>matching vehicles</div></div><div className="inventory__selects"><div className="inventory__select"><label>Brand</label><select value={brand} onChange={e=>setBrand(e.target.value)}>{brands.map(v=><option key={v}>{v}</option>)}</select><SlidersHorizontal size={14}/></div><div className="inventory__select"><label>Kilometers</label><select value={km} onChange={e=>setKm(e.target.value)}><option>Any mileage</option><option>Under 50,000</option><option>50,000–100,000</option><option>Over 100,000</option></select><SlidersHorizontal size={14}/></div><div className="inventory__select"><label>Engine type</label><select value={engine} onChange={e=>setEngine(e.target.value)}>{engines.map(v=><option key={v}>{v}</option>)}</select><SlidersHorizontal size={14}/></div></div>{hasExtraFilters&&<button className="inventory__clear" onClick={clearFilters}><X size={12}/> Clear filters</button>}</section>
    <div className="inventory__toolbar"><div className="inventory__filters">{(['All','SUV','Sedan','Coupe','Available'] as Filter[]).map(f=><button key={f} className={filter===f?'active':''} onClick={()=>setFilter(f)}>{f}</button>)}</div><div className="inventory__view-toggle"><button type="button" className={viewMode==='list'?'active':''} aria-label="List view" aria-pressed={viewMode==='list'} onClick={()=>setViewMode('list')}><List size={17}/></button><button type="button" className={viewMode==='grid'?'active':''} aria-label="Grid view" aria-pressed={viewMode==='grid'} onClick={()=>setViewMode('grid')}><Grid3X3 size={17}/></button></div></div>
    <div className={`inventory-list inventory-list--${viewMode}`}>{filtered.map(v=><article className="inventory-item" key={v.id}><button className="inventory-item__visual" onClick={()=>setSelected(v)}><img src={v.hero} alt={`${v.brand} ${v.model}`}/><span className="inventory-status">{v.status}</span></button><div className="inventory-item__main"><div className="inventory-item__identity"><small>{v.year} / {v.type} / {v.mileage.toLocaleString()} MI</small><h2>{v.brand}<br/><i>{v.model}</i></h2></div><div className="inventory-item__price"><small>ASKING PRICE</small><strong>{formatPrice(v.price)}</strong><span>{v.engine}</span></div></div><div className="inventory-item__specs"><span><b>{v.hp}</b> HP</span><span><b>{v.mileage.toLocaleString()}</b> MI</span><span><b>{v.year}</b> YEAR</span><button onClick={()=>setSelected(v)}>View vehicle</button></div></article>)}{!filtered.length&&<div className="inventory-empty">No vehicles match these filters.</div>}</div>
  </section>{selected&&<AnimatePresence mode="wait"><VehicleConfigurator vehicle={selected} onClose={()=>setSelected(null)}/></AnimatePresence>}</PageShell>
}
